# WebView JavaScript interface methods are kept automatically by R8's default
# WebView rules (android.webkit.JavascriptInterface). Nothing custom is needed
# for this app since it does not expose a JavascriptInterface.
